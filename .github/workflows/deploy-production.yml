name: Deploy Production

on:
  workflow_dispatch:
    inputs:
      deploy_target:
        description: What to deploy
        required: true
        type: choice
        options:
          - all
          - api-only
          - frontend-only
      framework:
        description: Active frontend framework
        required: false
        type: choice
        default: react
        options:
          - react
          - vue
          - svelte

permissions:
  contents: read

env:
  HOMELAB_DEPLOY_PATH: /opt/courier-service
  HOMELAB_DOMAIN: nurulizyansyaza.com
  ENVIRONMENT: production
  CORE_REF: main
  CLI_REF: main
  API_REF: main
  FRONTEND_REF: main

jobs:
  # ── Run Tests First ────────────────────────────────────────────────
  test:
    name: Run Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          repository: nurulizyansyaza/courier-service-core
          ref: ${{ env.CORE_REF }}
          path: courier-service-core
      - uses: actions/checkout@v4
        with:
          repository: nurulizyansyaza/courier-service-cli
          ref: ${{ env.CLI_REF }}
          path: courier-service-cli
      - uses: actions/checkout@v4
        with:
          repository: nurulizyansyaza/courier-service-api
          ref: ${{ env.API_REF }}
          path: courier-service-api
      - uses: actions/checkout@v4
        with:
          repository: nurulizyansyaza/courier-service-frontend
          ref: ${{ env.FRONTEND_REF }}
          path: courier-service-frontend

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Test Core
        run: cd courier-service-core && npm ci && npm run build && npm test

      - name: Test CLI
        run: cd courier-service-cli && npm ci && npm test

      - name: Test API
        run: cd courier-service-api && npm ci && npm test

      - name: Test Frontend
        run: |
          cd courier-service-frontend && npm ci
          echo '{"framework":"react"}' > framework.config.json
          node -e "
            const fs = require('fs');
            const tsconfig = JSON.parse(fs.readFileSync('tsconfig.json', 'utf-8'));
            tsconfig.compilerOptions.jsx = 'react-jsx';
            tsconfig.include = ['src/core', 'src/react', 'src/vite-env.d.ts'];
            fs.writeFileSync('tsconfig.json', JSON.stringify(tsconfig, null, 2) + '\n');
          "
          npm test

  # ── Deploy API (Docker build → SSH transfer → restart) ─────────────
  deploy-api:
    name: Deploy API
    needs: test
    runs-on: ubuntu-latest
    if: github.event.inputs.deploy_target != 'frontend-only'
    steps:
      - uses: actions/checkout@v4
        with:
          path: courier-service
      - uses: actions/checkout@v4
        with:
          repository: nurulizyansyaza/courier-service-core
          ref: ${{ env.CORE_REF }}
          path: courier-service-core
      - uses: actions/checkout@v4
        with:
          repository: nurulizyansyaza/courier-service-cli
          ref: ${{ env.CLI_REF }}
          path: courier-service-cli
      - uses: actions/checkout@v4
        with:
          repository: nurulizyansyaza/courier-service-api
          ref: ${{ env.API_REF }}
          path: courier-service-api

      - name: Set up SSH via Cloudflare Tunnel
        run: |
          curl -sL https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-amd64 -o /usr/local/bin/cloudflared
          chmod +x /usr/local/bin/cloudflared
          mkdir -p ~/.ssh
          echo "${{ secrets.HOMELAB_SSH_KEY }}" > ~/.ssh/id_ed25519
          chmod 600 ~/.ssh/id_ed25519
          cat > ~/.ssh/config <<EOF
          Host ${{ secrets.HOMELAB_HOST }}
            ProxyCommand /usr/local/bin/cloudflared access ssh --hostname %h
            StrictHostKeyChecking no
            UserKnownHostsFile /dev/null
          EOF
          chmod 600 ~/.ssh/config
        run: |
          IMAGE_TAG="courier-service-api:${{ github.sha }}"
          docker build \
            -f courier-service/Dockerfile \
            -t "$IMAGE_TAG" \
            -t "courier-service-api:latest" \
            .

      - name: Transfer image to homelab
        run: |
          docker save "courier-service-api:latest" | \
            ssh ${{ secrets.HOMELAB_USER }}@${{ secrets.HOMELAB_HOST }} "docker load"

      - name: Copy config files
        run: |
          scp courier-service/docker-compose.prod.yml \
            ${{ secrets.HOMELAB_USER }}@${{ secrets.HOMELAB_HOST }}:${{ env.HOMELAB_DEPLOY_PATH }}/courier-service/
          scp courier-service/infra/nginx/nginx.conf \
            ${{ secrets.HOMELAB_USER }}@${{ secrets.HOMELAB_HOST }}:${{ env.HOMELAB_DEPLOY_PATH }}/courier-service/infra/nginx/

      - name: Copy static pages
        run: |
          scp courier-service/static/index.html \
            ${{ secrets.HOMELAB_USER }}@${{ secrets.HOMELAB_HOST }}:${{ env.HOMELAB_DEPLOY_PATH }}/static/
          scp courier-service/static/cli.html \
            ${{ secrets.HOMELAB_USER }}@${{ secrets.HOMELAB_HOST }}:${{ env.HOMELAB_DEPLOY_PATH }}/static/

      - name: Restart API service
        run: |
          ssh ${{ secrets.HOMELAB_USER }}@${{ secrets.HOMELAB_HOST }} bash -s <<'EOF'
            set -euo pipefail
            cd ${{ env.HOMELAB_DEPLOY_PATH }}/courier-service
            docker compose -f docker-compose.prod.yml up -d --no-deps courier-api
            sleep 5
            curl -sf http://localhost:3000/api/health || exit 1
            echo "::notice::API deployed successfully"
          EOF

      - name: Reload Nginx
        run: |
          ssh ${{ secrets.HOMELAB_USER }}@${{ secrets.HOMELAB_HOST }} "sudo nginx -t && sudo systemctl reload nginx"

      - name: Print API URL
        run: echo "::notice::API deployed to https://${{ env.HOMELAB_DOMAIN }}/courier-service/api/health"

  # ── Deploy Frontend (Build all frameworks → rsync to homelab) ───────
  deploy-frontend:
    name: Deploy Frontend
    needs: test
    runs-on: ubuntu-latest
    if: github.event.inputs.deploy_target != 'api-only'
    steps:
      - uses: actions/checkout@v4
        with:
          path: courier-service
      - uses: actions/checkout@v4
        with:
          repository: nurulizyansyaza/courier-service-core
          ref: ${{ env.CORE_REF }}
          path: courier-service-core
      - uses: actions/checkout@v4
        with:
          repository: nurulizyansyaza/courier-service-frontend
          ref: ${{ env.FRONTEND_REF }}
          path: courier-service-frontend

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Set up SSH via Cloudflare Tunnel
        run: |
          curl -sL https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-amd64 -o /usr/local/bin/cloudflared
          chmod +x /usr/local/bin/cloudflared
          mkdir -p ~/.ssh
          echo "${{ secrets.HOMELAB_SSH_KEY }}" > ~/.ssh/id_ed25519
          chmod 600 ~/.ssh/id_ed25519
          cat > ~/.ssh/config <<EOF
          Host ${{ secrets.HOMELAB_HOST }}
            ProxyCommand /usr/local/bin/cloudflared access ssh --hostname %h
            StrictHostKeyChecking no
            UserKnownHostsFile /dev/null
          EOF
          chmod 600 ~/.ssh/config
        run: |
          cd courier-service-core && npm ci && npm run build
          cd ../courier-service-frontend && npm ci

      - name: Build and deploy React
        run: |
          cd courier-service-frontend
          npm run use:react
          VITE_API_BASE_URL=/courier-service/api npx vite build --base=/courier-service/frontend/react/
          rsync -az --delete dist/ \
            ${{ secrets.HOMELAB_USER }}@${{ secrets.HOMELAB_HOST }}:${{ env.HOMELAB_DEPLOY_PATH }}/frontend-builds/prod/react/

      - name: Build and deploy Vue
        run: |
          cd courier-service-frontend
          npm run use:vue
          VITE_API_BASE_URL=/courier-service/api npx vite build --base=/courier-service/frontend/vue/
          rsync -az --delete dist/ \
            ${{ secrets.HOMELAB_USER }}@${{ secrets.HOMELAB_HOST }}:${{ env.HOMELAB_DEPLOY_PATH }}/frontend-builds/prod/vue/

      - name: Build and deploy Svelte
        run: |
          cd courier-service-frontend
          npm run use:svelte
          VITE_API_BASE_URL=/courier-service/api npx vite build --base=/courier-service/frontend/svelte/
          rsync -az --delete dist/ \
            ${{ secrets.HOMELAB_USER }}@${{ secrets.HOMELAB_HOST }}:${{ env.HOMELAB_DEPLOY_PATH }}/frontend-builds/prod/svelte/

      - name: Reload Nginx
        run: |
          ssh ${{ secrets.HOMELAB_USER }}@${{ secrets.HOMELAB_HOST }} "sudo nginx -t && sudo systemctl reload nginx"

      - name: Print Frontend URL
        run: echo "::notice::Frontend deployed to https://${{ env.HOMELAB_DOMAIN }}/courier-service/frontend/"
